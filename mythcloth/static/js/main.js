// Generated by CoffeeScript 1.9.0
$(document).ready(function() {
  var days, itemsData, total;
  total = 0;
  days = [];
  $('.highchart thead').find('th').not(':nth-child(1)').each(function() {
    var day;
    day = $(this).html();
    return days.push(day);
  });
  console.log('days');
  console.log(days);
  itemsData = [];
  $('.highchart tbody').find('tr').each(function() {
    var name, priceData;
    name = $(this).find('td:nth-child(1)').html();
    priceData = [];
    $(this).find('td').not(':nth-child(1)').each(function() {
      var price;
      price = $(this).html();
      price = parseFloat(price);
      return priceData.push(price);
    });
    console.log('priceData');
    console.log(priceData);
    itemsData.push({
      name: name,
      data: priceData
    });
  });
  console.log('itemsData');
  console.log(itemsData);
  return $('#highchart-container').highcharts({
    title: {
      text: ''
    },
    xAxis: {
      categories: days
    },
    yAxis: {
      title: {
        text: ''
      }
    },
    series: itemsData,
    legend: {
      layout: 'vertical',
      align: 'right',
      verticalAlign: 'middle',
      borderWidth: 0
    },
    chart: {
      height: 1500
    }
  });
});
